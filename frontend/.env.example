# Copy to .env (git-ignored). On Vercel, set this under Project Settings → Environment Variables.

# The backend's address, without a trailing slash. Baked in at build time, so rebuild after
# changing it. Production builds fail if it is missing.
VITE_API_BASE_URL=http://localhost:8080
