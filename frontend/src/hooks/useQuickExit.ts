/**
 * Quick exit now lives in `@/features/quick-exit`, where the destination, shortcut, and erase
 * behaviour are configurable. This re-export keeps existing imports working.
 */
export { quickExit } from '@/features/quick-exit/quickExit'
