<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Backstop: drops any log event that looks like it carries a session ID, IP, or email. -->
    <turboFilter class="com.sanctum.common.logging.SensitiveDataTurboFilter"/>

    <springProfile name="!prod">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <!-- Never include caller data or thread-bound context beyond the request ID. -->
                <includeContext>false</includeContext>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
</configuration>
